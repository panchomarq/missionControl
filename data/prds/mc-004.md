# PRD: Mostrar roadmap de Obsidian en vista de proyecto

## Task ID
mc-004

## Objetivo
Cuando existe `data/scans/{slug}-obsidian.json`, mostrar el roadmap en la página de detalle.

## IMPORTANT — Retry context
This task was rejected because the previous attempt:
- Rewrote `page.tsx` entirely, replacing all existing code with placeholders
- Rewrote `data.ts` entirely, losing all existing functions
- Did not import the ObsidianRoadmap type in RoadmapPanel

DO NOT rewrite existing files. Add to them.

## Archivos

### 1. CREAR `components/RoadmapPanel.tsx` (NEW FILE)

```typescript
import type { CSSProperties } from "react";
import type { ObsidianRoadmap } from "@/lib/data";
import { Panel } from "@/components/Panel";

const progressStyle: CSSProperties = {
  fontFamily: "var(--font-body)",
  fontSize: "var(--ft-sm)",
  color: "var(--text-dim)",
  marginBottom: 8,
};

const rowStyle: CSSProperties = {
  display: "flex",
  alignItems: "center",
  gap: 8,
  padding: "4px 0",
  fontFamily: "var(--font-body)",
  fontSize: "var(--ft-base)",
};

interface RoadmapPanelProps {
  roadmap: ObsidianRoadmap;
}

export function RoadmapPanel({ roadmap }: RoadmapPanelProps) {
  const done = roadmap.tasks.filter((t) => t.status === "done").length;

  return (
    <Panel title="Obsidian Roadmap" style={{ marginTop: 16 }}>
      <div style={progressStyle}>
        {done}/{roadmap.tasks.length} completed
      </div>
      {roadmap.tasks.map((task, i) => {
        const isDone = task.status === "done";
        return (
          <div key={i} style={rowStyle}>
            <span style={{ color: isDone ? "var(--health-green)" : "var(--text-dim)" }}>
              {isDone ? "●" : "○"}
            </span>
            <span style={{
              color: isDone ? "var(--text-dim)" : "var(--text)",
              textDecoration: isDone ? "line-through" : "none",
              opacity: isDone ? 0.5 : 1,
            }}>
              {task.title}
            </span>
          </div>
        );
      })}
    </Panel>
  );
}
```

Write this file EXACTLY as shown above. Do not change anything.

### 2. ADD to `lib/data.ts` (MODIFY — append only)

Add these lines AT THE END of the existing file. Do NOT remove or change any existing code.
The file currently ends with the `getAllPrds` function. Add after it:

```typescript
export interface ObsidianRoadmap {
  projectId: string;
  sourceFile: string;
  tasks: Array<{
    title: string;
    status: "pending" | "done";
    source: "obsidian";
  }>;
}

export async function getObsidianRoadmap(
  slug: string,
): Promise<ObsidianRoadmap | null> {
  const filePath = join(DATA_DIR, "scans", `${slug}-obsidian.json`);
  try {
    const raw = await readFile(filePath, "utf-8");
    return JSON.parse(raw);
  } catch {
    return null;
  }
}
```

### 3. ADD to `app/project/[slug]/page.tsx` (MODIFY — 3 small changes)

The current file has these imports at the top:
```
import { getProject, getTasksByProject, getTaskPrd, getAgents } from "@/lib/data";
```

Change 1: Add to that import line:
```
import { getProject, getTasksByProject, getTaskPrd, getAgents, getObsidianRoadmap } from "@/lib/data";
```

Change 2: Add this import after the existing imports:
```
import { RoadmapPanel } from "@/components/RoadmapPanel";
```

Change 3: Inside the page function, after `const allAgents = await getAgents();` add:
```
const roadmap = await getObsidianRoadmap(slug);
```

Change 4: At the end of the JSX, after the Quests Panel closing tag `</Panel>`, add:
```
{roadmap && <RoadmapPanel roadmap={roadmap} />}
```

DO NOT rewrite the entire page.tsx. The file is 205 lines. Output it COMPLETE with all existing code plus these 4 additions.

## Reference — current `lib/data.ts` ending

The file currently ends with:
```typescript
export async function getAllPrds(
  tasks: Task[],
): Promise<Record<string, string>> {
  // ... existing code
}
```

Your additions go AFTER this function.

## Criterio de éxito
- `/project/missionControl` shows the roadmap panel if the obsidian JSON exists
- No existing functionality broken
- No TypeScript errors
